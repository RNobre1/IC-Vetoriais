---
tipo: log-de-sessão
data: 2026-05-05
foco: fechar Etapa 1 + executar Etapa 2 / Dia 1
tags: [sessão, etapa-1, etapa-2, dia-1, checkpoint]
---

# Sessão 2026-05-05 — Etapa 1 fechada + Etapa 2 / Dia 1

## TL;DR — onde paramos
**Etapa 2 / Dia 1 ✅** — esqueleto do `code/` pronto, docker-compose dos 3 SGBDs com todos os containers `healthy`, smoke test integrado 6/6 verde, CI no ar.

**Próximo passo concreto:** Etapa 2 / Dia 2 — `pipeline/ms_marco_loader.py` (download + sampling determinístico) + `pipeline/embeddings.py` (geração + cache) + os 3 seeders, todos via TDD. Detalhes em [`../../docs/tasks/etapa-2-preparacao-ambiente.md`](../../docs/tasks/etapa-2-preparacao-ambiente.md).

## O que foi entregue nesta sessão

### Etapa 1 — fechamento de lacunas
- 5 templates em `vault/_templates/` (paper, decisão, referência, experimento, draft).
- 6 ADRs em `vault/decisões/` (sistemas, HNSW, modelo, dataset, tamanhos, cenários).
- 6 notas conceituais em `vault/referência/` (VSM, embeddings densos, ANN/HNSW, VDBMS, RAG, metodologia de benchmarking).
- 13 fichamentos em `vault/papers/` — todos com `citacao_abnt` alinhado literalmente à lista de Referências do `docx/relatorio_parcial.docx`.
- `.gitignore` ajustado para versionar `.md` em `vault/papers/` mantendo PDFs/HTML ignorados.
- 1 lição: [[../lições/2026-05-05-rigor-citacoes-abnt]].

### Etapa 2 — Dia 1
- `code/`: `pyproject.toml`, `requirements.txt`, `requirements.lock`, `Makefile`, `README.md`, `.env.example`, `docker-compose.yml`.
- `code/tests/unit/test_basic.py` (2 testes) e `code/tests/integration/test_smoke.py` (6 testes integrados).
- `.github/workflows/test.yml` — CI lint + unitários em push/PR pra `main`.
- ADR: [[../decisões/2026-05-05-versoes-imagens-docker]].
- Lição: [[../lições/2026-05-05-armadilhas-dia-1-etapa-2]] (httpx vs weaviate, psycopg-binary sem wheel py3.14, gRPC do Weaviate).

### Regras do projeto registradas
- Não editar `.docx` diretamente; alterações em binários passam por editor externo.
- Rigor científico: todo artefato precisa rastreabilidade a fonte e alinhamento ao docx.

## Estado da infraestrutura ao fim da sessão

### Containers Docker
| Container     | Imagem                              | Status   | Portas no host             |
|---------------|-------------------------------------|----------|----------------------------|
| ic-pgvector   | `pgvector/pgvector:pg16`            | healthy  | 5432                       |
| ic-qdrant     | `qdrant/qdrant:v1.12.0`             | healthy  | 6333 (REST), 6334 (gRPC)   |
| ic-weaviate   | `semitechnologies/weaviate:1.27.0`  | healthy  | 8080 (REST), 50051 (gRPC)  |

Volumes Docker (named, gerenciados pelo daemon, vazios além da inicialização): `code_pgdata`, `code_qdrantdata`, `code_weaviatedata`.

### Ambiente Python
- venv em `code/.venv` instalado a partir de `code/requirements.txt`.
- `code/requirements.lock` captura todas as versões transitivas resolvidas (gerado via `pip freeze`).
- `code/.env` criado a partir de `.env.example` (defaults funcionam).

### Decisões metodológicas pendentes (a virar ADR durante a execução)
- Estratégia de chunking para passages do MS MARCO (default: passage como vem).
- Ampliação do CI para integração via `services:` do GitHub Actions (avaliação no Dia 4).

## Como retomar a próxima sessão
1. `cd "/home/rnobre/Área de trabalho/UFOPA/IC-Vetoriais"` e `pwd` para confirmar.
2. `git log --oneline -15` para conferir o estado do código.
3. Conferir containers: `cd code && docker compose ps`. Se algum não estiver `healthy`, `make up` (ou `make down && make up`).
4. Ativar venv: `source code/.venv/bin/activate` (ou usar `.venv/bin/...` direto).
5. Smoke check rápido: `make smoke` (deve continuar verde 6/6).
6. Abrir [`../../docs/tasks/etapa-2-preparacao-ambiente.md`](../../docs/tasks/etapa-2-preparacao-ambiente.md) na seção **Dia 2** para a lista de entregáveis.

## Convenções estabelecidas nesta sessão
- Frontmatter padronizado nos templates de `vault/_templates/`.
- Filenames de fichamentos no formato `Sobrenome[-Sobrenome2]-Ano-Tema.md` (ano = ano da publicação real, não da nossa citação).
- ADRs datadas em `YYYY-MM-DD-tema.md`.
- Lições datadas em `YYYY-MM-DD-tema.md`.
- Logs de sessão em `YYYY-MM-DD.md` neste diretório.

## Backlinks
- [[../decisões/2026-05-05-versoes-imagens-docker]]
- [[../lições/2026-05-05-rigor-citacoes-abnt]]
- [[../lições/2026-05-05-armadilhas-dia-1-etapa-2]]
- [[../../docs/tasks/etapa-2-preparacao-ambiente]]
